{
  "name": "gitview-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitView bridge — serves a git repo read-only and drives/attaches to Claude sessions for the GitView Android app.",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "@fastify/cors": "^10.0.0",
    "fastify": "^5.0.0",
    "ws": "^8.18.0",
    "yaml": "^2.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0"
  },
  "//": "Pin @anthropic-ai/claude-agent-sdk to the current version and verify its exported option names before relying on the read-only permission profile (see docs/SECURITY.md)."
}
