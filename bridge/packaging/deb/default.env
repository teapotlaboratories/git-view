# Environment for gitview-bridge.service (see `man systemd.exec`).
# If node is not on the system PATH, point at it here:
#GITVIEW_NODE=/usr/bin/node
# Override the config path if needed:
#GITVIEW_CONFIG=/etc/gitview-bridge/config.yaml
