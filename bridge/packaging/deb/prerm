#!/bin/sh
set -e
case "$1" in
  remove|deconfigure)
    if [ -d /run/systemd/system ]; then
      systemctl stop gitview-bridge.service >/dev/null 2>&1 || true
      systemctl disable gitview-bridge.service >/dev/null 2>&1 || true
    fi
    ;;
esac
exit 0
